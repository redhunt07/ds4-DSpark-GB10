Write a Python function `find_anagram_groups(words: list[str]) -> list[list[str]]` that groups the input words by anagram equivalence. Words with the same sorted character sequence belong to the same group. Sort each group alphabetically, sort the outer list by the first word of each group, and return only groups with at least 2 members. Use a dict keyed by the sorted character tuple for O(N * K log K) overall. Include a docstring, example usage, and three test assertions. Write the complete function with no placeholder comments.