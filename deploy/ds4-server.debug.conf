[Service]
# Production diagnostics: retain event metadata and prompt hashes, never the
# request/response payload or one-line-per-speculation logging.
Environment=DS4_PROMPT_DIAGNOSTICS=1
Environment=DS4_HTTP_DEBUG=1
Environment=DS4_SERVER_BUFFER_TOOL_STREAM=1
